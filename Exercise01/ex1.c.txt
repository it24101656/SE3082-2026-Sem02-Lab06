#include omp.h
#include stdio.h

int main() {

#pragma omp parallel
{
  int th = omp_get_thread_num();
  printf(thread # %dn, th);

 #pragma omp barrier

 printf(Outside Barrier # %dn, omp_get_thread_num());
 printf(Hi again from thread # %dn, omp_get_thread_num());

}

   return 0;
}
